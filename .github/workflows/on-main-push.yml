name: Main

on:
  push:
    branches:
      - main


jobs:
  tests:
    uses: ./.github/workflows/run-tests.yml
    secrets: inherit

#  buildDocker:
#    needs:
#      - tests
#    uses: ./.github/workflows/build-docker.yml
#    secrets: inherit